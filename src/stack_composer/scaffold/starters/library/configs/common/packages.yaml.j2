# TODO(scaffold): Replace package defaults with site-approved policy.
# Rendered for {{ profile.system.name }} / {{ stack.name }}
packages:
  all:
    prefer:
      - target={{ rendered_lanes[0].target }}
