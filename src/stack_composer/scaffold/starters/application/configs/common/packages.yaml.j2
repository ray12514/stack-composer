# TODO(scaffold): Replace application package defaults with site policy.
# Rendered for {{ profile.system.name }} / {{ stack.name }}
packages:
  all:
    prefer:
      - target={{ rendered_lanes[0].target }}
