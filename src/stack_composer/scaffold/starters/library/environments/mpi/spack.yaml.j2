# TODO(scaffold): Review MPI provider policy for this lane.
# Rendered by {{ renderer_identity.name }} {{ renderer_identity.version }}
spack:
  view: {{ view_root }}
  include:
{% for scope in scopes %}
    - {{ scope }}
{% endfor %}
  specs:
{% for spec in specs %}
    - {{ spec }}
{% endfor %}
